# Proposed patch — greenhouse (1781982818)

**Target file:** `backend/app/browser_automation/adapters/greenhouse.py`

**Rationale:** The _upload_greenhouse_files method is truncated mid-implementation. The file classification logic is complete, but the actual upload calls are missing. This causes fill_form to fail when file fields are present because the files are never actually uploaded to the form. The patch completes the method by adding positional fallback logic and the missing upload_file calls for both resume and cover letter.

## How to apply (after human review)

```bash
git apply greenhouse_1781982818.diff
```

This patch was authored by Gemini. Review carefully before applying.
